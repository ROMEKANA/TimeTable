# 作業ルール

## Git運用

- Gitの実操作はユーザーが行う。
- Codexは `git add`、`git commit`、`git push` などのGit書き込み操作を原則実行しない。
- 変更が一区切りついたら、Codexはコミットするタイミングと日本語コミットメッセージ案を提示する。
- コミットメッセージ案は、コピペしやすいようにメッセージ本文だけをコードブロックで出す。
- コミットメッセージは日本語で、変更内容が分かる短い文にし、先頭に変更後のバージョン番号を含める。例：`v0.2.21 変更内容`
- 新しいプロジェクトを作ったら、最初にGitリポジトリとして初期化することを提案する。
- ビルド成果物、一時ファイル、ローカル環境依存ファイルはコミットしない。そのための `.gitignore` も用意する。
- 既存のユーザー変更を勝手に戻さない。
- コミット前に `git status` を確認し、意図したファイルだけを含めるよう案内する。

## 作業姿勢

- まず既存の構成と習慣を確認してから変更する。
- 変更は目的に対して小さく保つ。
- 動作確認できる場合は確認してから完了報告する。
- エラーや制約がある場合は、原因と次に取れる手を短く説明する。
- 自分でできることは、なるべく追加確認を待たずに実行して報告する。
- 覚えておくべき追加の指示は、適宜このファイルにまとめる。

## コード編集

- 読みやすさを優先し、不要な抽象化は増やさない。
- 設定で変えられると便利な値は、コード直書きより設定ファイル化を検討する。
- ユーザーが編集する設定ファイルは、例を分かりやすく保つ。
- コメントは必要な場所にだけ短く書く。
- 文字化けして見えるのは、ユーザーには正常に見えていることがある。削除や変更する前に、文字コードの設定を見直したり、ユーザーに確認したりすること。基本的にUTF-8を使用。
- 勝手な削除は慎む。コードの削除の際は、コメントアウトするようにする。
- もう一度言う、コードの削除はコメントアウトで対応する。
- 関数宣言は一行にまとめる。コメントで説明を付ける。
- 関数を実装する際は、宣言の順番に従う。
- 関数宣言せずに実装する場合は、上の行にコメントで説明を付ける。

## 検証

- 可能ならビルドやテストを実行する。
- 実行できなかった検証がある場合は、その理由を報告する。
- ゲームModなど外部アプリ上での確認が必要な場合は、確認手順とログ取得方法も添える。

## プロジェクト固有の作業ルール

- Codexがファイルを変更するたびに、`CMakeLists.txt` のプロジェクトバージョンも更新する。
- 最後の確認では、次のDebugビルドだけを実行する。確認目的で `make-release-zip.bat` は実行しない。
- `TimeTable.exe` の実画面・実操作の確認は基本的にユーザーが行う。CodexはDebugビルドとコード上の確認を行い、ユーザーが確認すべき操作手順や注意点を短く伝える。

```powershell
$env:PATH='C:\Qt\Tools\mingw1310_64\bin;C:\Qt\6.11.0\mingw_64\bin;' + $env:PATH
& 'C:\Qt\Tools\CMake_64\bin\cmake.exe' --build 'C:/Users/TO/Documents/jukuTimeTable/TimeTable/build/Desktop_Qt_6_11_0_MinGW_64_bit-Debug' --target all
```

- `TimeTable.exe` が起動中だと、リンク時に `cannot open output file TimeTable.exe: Permission denied` で失敗する。アプリは勝手に終了せず、起動中であることをユーザーへ伝える。
- リリース方法は `README.md` を参照するようユーザーに伝える。
- 講師の毎日の業務には、授業後の指導報告書のスキャン、PDFの分割・名前付け、生徒への送付までを含む。講師向けマニュアルや業務フローを変更するときは、この一連の作業を省略しない。

### Qt/MinGW環境の注意

- 2026-07-02時点では、Qt/MinGWビルドが診断なしの `FAILED: [code=1]` で停止していた。
- 2026-07-03に、MinGW内部コンパイラが必要DLLを見つけられないことが原因と判明した。`cc1plus.exe --version` が `-1073741515` で落ちる場合は、ビルド前にPATH先頭へ `C:\Qt\Tools\mingw1310_64\bin` と `C:\Qt\6.11.0\mingw_64\bin` を追加する。Qt Creator側のビルド環境にも同じ2パスを追加する。

## 今後やりたいこと

- 各操作に分かりやすいツールチップを追加する。

## バグ候補・完成前に確認したいこと

優先度は、P1がデータ損失につながる最優先、P2が誤動作や重要な安全性の問題、P3が設計改善・性能改善を表す。

### 2026-09-03 全体チェック

- 指定のDebugビルドは成功した。
- `ctest` ではテストが見つからず、現在も自動テストは登録されていない。
- 現コードで、既存の「通常の `QFile` による直接上書き」「壊れた `master.json` の初期値上書き」「不正な時間割JSONの検証不足」「自動更新時の強制終了」「時間割構成変更時の `resize()` によるデータ欠落・対応ずれ」「不正なクリップボード内容によるセル消去」が未解決であることを再確認した。詳細と修正方針は下記のP2・P3項目を参照する。
- データ損失につながる保存・読込処理と自動更新の安全化を、機能追加より先に進める。

### P1：最優先

### P2：重要

- `MaxStudentPerTeacher`、曜日、時限を減らす操作は `resize()` で後ろの授業データを落とす。Undo履歴の破棄だけでなく、削除される実データを示した明示確認を追加する。
- 講師列の削除も列内の授業データを消す。削除対象の曜日・講師名・授業件数を示した確認を追加する。
- マスターデータ変更後の `refreshAfterMasterDataChanged()` は生徒編集欄を `clearStudentEntry()` する。生徒編集中にマスターを編集すると保存前の内容が消えるため、マスター変更前に `confirmStudentEditorChanges()` を通し、キャンセル時は変更処理を中止できるようにする。
- 無効なクリップボード内容をセルへ貼り付けると、`jsonToLesson()` が空の `LessonData` を返したあとも処理が続き、選択セルを空データで上書きする。変換処理から成功・失敗を別に返し、失敗時はセルを変更しない。
- 自動更新では、TimeTableへ終了要求を出したあと20秒経過すると `Stop-Process -Force` を実行する。保存確認をキャンセルした場合や回答が間に合わない場合も、未保存の時間割が失われる。アプリが終了しなければ更新を中止する。
- schedule/master/students/teachers/school/appState の保存は通常の `QFile` で既存ファイルを直接上書きし、`write()` の戻り値も確認していない。途中失敗やディスク容量不足でファイルが壊れる可能性があるため、`QSaveFile` で原子的に保存する。
- 壊れた `master.json` は、起動時に空オブジェクトとして正規化され、初期値で上書きされる。壊れたファイルを退避して警告し、復元できるようにする。
- 壊れた `.schedule` を読み込むと、警告後に新規週として表示される経路がある。直後の保存で壊れたファイルを上書きしないよう、破損ファイルの退避や「新規として開いている」表示を追加する。
- 生徒名・講師名の重複チェックがない。予定表、敬称、教材、給与、一覧切替の多くが「学年+生徒名」または「講師名」で検索するため、同名データがあると別人の情報を拾う。安定したIDを持たせるか、保存時に重複を禁止・警告する。
- 生徒/講師一覧の項目切替時の自動保存は、保存後に名前で選択先を探し直す。同名がある場合、意図した行ではなく先に見つかった行を読み込む可能性がある。重複名対策と一緒に見直す。
- 曜日/時限の編集では、編集欄の未反映セル内容に `updateCell()` を呼ばずに表を組み替える。曜日/時限変更前に選択セルの編集内容を反映する。
- 生徒一覧の保存・削除後は時間割側の生徒コンボの現在値を維持するが、講師一覧の保存・削除後に更新される講師コンボには同じ保護がない。候補から消えた講師名が空白になり、直後の講師列名変更で列名を空にする可能性がある。
- `updateCell()` が印刷、PDF、給与、予定表コピー、生徒/講師一覧からのコピーなど多数の入口で無条件に呼ばれる。時間割編集欄の表示が古くなったり空白化した場合、選択中セルへ意図せず書き戻す可能性がある。編集欄のdirty管理か、時間割タブ外での更新条件を整理する。
- 講師向け日別印刷のヘッダ曜日は、対象週ファイルの `days` ではなく現在開いている時間割の `days` を参照する。週ごとに曜日ヘッダが違う場合、対象週の曜日を使う。
- 指導報告書の日付欄の先頭に特殊空白 `ㅤ`（U+3164）が2文字あり、中央揃えでも見える文字が右へずれ、フォントによっては欠落記号になる。特殊空白を削除して配置を中央揃えへ任せる。
- 講師向け日別印刷は、同じ生徒の次回日の授業をすべて固定高さの枠へ描画する。同日授業やメモが多い場合に文字が切れるため、描画前の高さ判定、文字縮小、ブロック高さ調整のいずれかを追加する。
- `jsonToScheduleData()` は `monday` と `schedule` キーの存在しか確認せず、`days` / `periods` の欠落・空配列や `schedule` の型不正を成功として扱う。その結果、破損・旧形式ファイルが空の時間割として開かれ、保存で元データを上書きする可能性がある。必須配列の型・空でないこと・配列数の整合性を検証する。
- `loadStudent()`、`loadTeacher()`、`loadSchoolList()` はJSONルートがオブジェクトであれば、必須配列が欠落した `{}` なども空データとして受け入れる。読み込み前に現在のメモリ上データも `clear()` するため、スキーマ検証後に一時データへ読み込み、成功時だけ差し替える。
- 生徒・講師の削除と講師の更新は、メモリ上の `allStudents` / `teachers` を変更してから保存する。保存失敗時にロールバックしないため、ファイルと一覧モデルとメモリの対応が崩れ、後の保存で意図しない削除を確定する可能性がある。コピーを保存し、成功後にメンバーへ反映する。
- `redoCellEdit()` は `undoCellEdit()` と違って先に `updateCell()` を呼ばない。Undo後に編集欄を変更してそのままRedoすると、新しい編集を履歴に入れず古いRedo内容で上書きする。Redo前に編集を反映し、新規編集時はRedo履歴を破棄する。
- 指導報告書PDFの自動入力・手入力内容は、日付または講師の選択を変えると確認なしで `guidanceReportPdfEntries` を作り直して失われる。複数ページ入力中の切り替え前にdirty確認を入れる。
- 指導報告書PDFの名前変更はページごとに出力先へ `QFile::rename()` する。途中の1件が失敗すると先行ページだけが残り、再実行で連番の重複ファイルが生成される。出力先の一時フォルダで全件を準備し、失敗時は全件を後始末するトランザクション的な処理にする。
- 更新アプリはダウンロードしたZIPの書き込み結果・サイズ・ハッシュ・必須ファイルを検証せず、展開直後に既存インストール先へコピーする。書き込み不足や誤ったZIPアセットで中途半端なインストールになるため、検証後にバックアップ・切り替えを行う。
- 起動時は `setupScheduleTab()` が生徒・講師データの読み込みより先に実行されるため、最初に選択された時間割セルの学年・講師コンボが空のままになる。編集モードへ切り替えて別セルへ移動すると学年を空で書き戻し、講師列名変更では列名を空にする可能性があるため、マスターデータ読み込み後に時間割編集欄を再表示するか初期化順を変更する。
- 曜日・時限の一覧編集は、要素の追加位置や並べ替えを考慮せず配列を同じインデックスのまま `resize()` する。途中への追加・削除・並べ替えで既存授業が別の曜日・時限として扱われるため、旧見出し名との対応付けや変更内容の確認を行ってからデータを移行する。
- 時間割の日付は曜日名に関係なく `scheduleMonday.addDays(dayIndex)` で決まる。月曜以外から始める、途中の曜日を省く、曜日を並べ替えると、表示曜日と実際の日付がずれて予定表・給与・指導報告書へ波及するため、曜日を固定するか各曜日に日付オフセットを持たせる。
- 授業メモが空の場合の自動補完は、既存メモを意図的に削除した場合にも `recentLessonMemo()` で過去メモを戻す。その設定が有効な間はメモだけを空にできないため、新規授業への初回補完とユーザーによる削除を区別する。
- 給与計算は ` ()` が偽のセルを授業として数えるため、生徒名がなくメモ・学年・教科だけが入った未完成セルでも1:1または1:2のコマ給が発生する。給与対象は生徒名など必須項目が揃った授業に限定し、不完全セルを警告する。
- 給与明細は対象月の週ファイルが存在しない場合と、存在するが読めない・壊れている場合を区別せず `continue` し、その週を0件として計算する。破損週を含む明細を無警告で印刷しないよう、読み込みエラーを収集して計算を中止する。
- 任意の場所から開いた `.schedule` の元パスを保持せず、保存と未保存判定は常に `schedules/<内部の月曜日>.schedule` を対象にする。開いたファイル自体は更新されず、同じ週の別ファイルを上書きする可能性があるため、「インポート」として明示するか現在の保存先を追跡する。
- 閲覧モード中でも「設定」から `MaxStudentPerTeacher` を変更でき、`refreshAfterMasterDataChanged()` が時間割を `resize()` して履歴も消す。編集ロックを構造変更にも適用し、人数削減時のデータ削除確認と合わせて保護する。
- 生徒の性別は名称ではなくコンボボックスのインデックスで保存している。性別マスターを並べ替え・削除すると既存生徒の性別と敬称判定が別の値へ変わるため、安定した値で保存するかマスター変更時に既存データを移行する。

### P3：改善候補

- `scheduleMatchesSavedFile()` はJSON文字列の完全一致で未保存判定する。内容が同じでも整形や旧保存形式の違いで保存確認が出る可能性があるため、必要なら構造比較へ変更する。-変更不要
- 「この週を来週にコピー」はコピー先が「表示週の翌週」ではなく「今日基準の来週」になっている。過去・未来の週を開いている場合にUI文言と期待がずれるため、仕様と文言を揃える。
- 生徒予定表・講師予定表のコピーは、現在開いている週の `scheduleEntries()` を使う。対象週選択や複数週出力を増やす場合は、対象週ファイルを明示的に読み込む設計へ寄せる。
- 講師向け日別印刷の「次回予定」「教科予定」は、生徒ごとに最大8週分のファイルを繰り返し読み込む。印刷人数が多い場合の遅延を避けるため、読み込んだ週データを処理中にキャッシュする。
- 現在、自動テストは登録されていない。貼り付け、保存失敗、週コピー、JSON読み込みなどデータ損失につながる処理からテストを追加する。
- 指導報告書PDFで不正なPDFを選ぶと、以前のページ表示名やボタン状態が画面に残る。読み込み失敗時にPDF作業状態と表示を初期化する。
- 指導報告書PDFの分割は `QProcess::waitForFinished(-1)` で完了を待つため、処理中は画面が固まって見える。非同期実行と進捗表示を検討する。
- ~~学年・性別・教科のマスター一覧編集は、Undo/Redo履歴があると `confirmClearCellEditHistory()` をラベル名と「マスターデータの管理」の2回連続で表示する。一度の確認にまとめる。~~ v0.3.21で統合設定画面と単一保存フローへ移行して解決。
- 学校一覧の追加・削除は `saveSchoolList()` が `void` のため、保存エラー後も画面を更新して「追加しました」「削除しました」と表示する。成否を返し、失敗時はメモリと表示を元に戻す。
- 更新アプリは空の `QUrl` に対して `isValid()` だけを確認している。空URLも有効と判定され得るため、リリースにZIPや `html_url` がない場合でも更新・リリースページボタンが有効になる。`isEmpty()`、HTTPSスキーム、想定ホストも合わせて検証する。
- 更新ファイル用フォルダの作成失敗またはZIPファイルのオープン失敗で早期returnすると、再確認・更新ボタンを無効にしたまま進捗表示も残る。全終了経路でUI状態を復元し、一時フォルダも後始末する。
- `make-release-zip.bat` は既存のReleaseビルドフォルダ直下をそのまま圧縮するため、過去ビルドの不要DLL・プラグイン・未除外ファイルが配布物へ残り得る。空のステージングフォルダへ必要物だけを配置してからZIP化する。
- 時間割PDF出力は出力先フォルダの作成結果や `QPrinter` のエラーを確認せず、描画失敗時も「保存しました」と表示する。生成後のエラー状態とファイルの存在・サイズを確認してから成功表示する。
- 指導報告書PDFタブの講師一覧更新は、同じ講師を再選択できた場合にシグナルを遮断したまま自動入力一覧を再生成しない。時間割を変更してタブへ戻っても古い生徒名・教科が残るため、手入力のdirty管理を導入したうえで再読込か保持かを明示する。
